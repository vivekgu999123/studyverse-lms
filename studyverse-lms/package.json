{
  "name": "studyverse-lms",
  "version": "2.0.0",
  "description": "StudyVerse \u2014 Full University LMS",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "create-admin": "node scripts/createAdmin.js",
    "seed": "node -e \"require('dotenv').config();const mongoose=require('mongoose');mongoose.connect(process.env.MONGO_URI).then(async()=>{const r=await fetch('http://localhost:5000/api/subjects/seed',{method:'POST'});console.log(await r.json());process.exit(0)})\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "csv-parse": "^5.5.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.3.1",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}